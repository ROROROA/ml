project: movielens_recommendations
provider: local

online_store:
    type: redis
    connection_string: localhost:6379
registry:
    path: postgresql+psycopg://feast:feast@localhost:5433/feast
    registry_type: sql
    cache_ttl_seconds: 60
    sqlalchemy_config_kwargs:
        echo: false
        pool_pre_ping: true
auth:
    type: no_auth
entity_key_serialization_version: 3
